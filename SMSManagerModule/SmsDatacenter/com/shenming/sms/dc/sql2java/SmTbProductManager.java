// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.shenming.sms.dc.sql2java;

import java.sql.*;
// imports+ 

// imports- 

/**
 * Handles database calls for the sm_tb_product table.
 */
public class SmTbProductManager
// extends+ 

// extends- 
{

    /**
     * Column prd_id of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_PRD_ID = 0;
    public static final int TYPE_PRD_ID = Types.DECIMAL;
    public static final String NAME_PRD_ID = "prd_id";

    /**
     * Column prd_type_id of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_PRD_TYPE_ID = 1;
    public static final int TYPE_PRD_TYPE_ID = Types.DECIMAL;
    public static final String NAME_PRD_TYPE_ID = "prd_type_id";

    /**
     * Column name of type Types.VARCHAR mapped to String.
     */
    public static final int ID_NAME = 2;
    public static final int TYPE_NAME = Types.VARCHAR;
    public static final String NAME_NAME = "name";

    /**
     * Column price of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_PRICE = 3;
    public static final int TYPE_PRICE = Types.DECIMAL;
    public static final String NAME_PRICE = "price";

    /**
     * Column price_vip of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_PRICE_VIP = 4;
    public static final int TYPE_PRICE_VIP = Types.DECIMAL;
    public static final String NAME_PRICE_VIP = "price_vip";

    /**
     * Column suply_status of type Types.VARCHAR mapped to String.
     */
    public static final int ID_SUPLY_STATUS = 5;
    public static final int TYPE_SUPLY_STATUS = Types.VARCHAR;
    public static final String NAME_SUPLY_STATUS = "suply_status";

    /**
     * Column format_desc of type Types.VARCHAR mapped to String.
     */
    public static final int ID_FORMAT_DESC = 6;
    public static final int TYPE_FORMAT_DESC = Types.VARCHAR;
    public static final String NAME_FORMAT_DESC = "format_desc";

    /**
     * Column detail_desc of type Types.VARCHAR mapped to String.
     */
    public static final int ID_DETAIL_DESC = 7;
    public static final int TYPE_DETAIL_DESC = Types.VARCHAR;
    public static final String NAME_DETAIL_DESC = "detail_desc";

    /**
     * Column send_area of type Types.VARCHAR mapped to String.
     */
    public static final int ID_SEND_AREA = 8;
    public static final int TYPE_SEND_AREA = Types.VARCHAR;
    public static final String NAME_SEND_AREA = "send_area";

    /**
     * Column send_desc of type Types.VARCHAR mapped to String.
     */
    public static final int ID_SEND_DESC = 9;
    public static final int TYPE_SEND_DESC = Types.VARCHAR;
    public static final String NAME_SEND_DESC = "send_desc";

    /**
     * Column prd_pic_file of type Types.VARCHAR mapped to String.
     */
    public static final int ID_PRD_PIC_FILE = 10;
    public static final int TYPE_PRD_PIC_FILE = Types.VARCHAR;
    public static final String NAME_PRD_PIC_FILE = "prd_pic_file";

    /**
     * Column owner of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_OWNER = 11;
    public static final int TYPE_OWNER = Types.DECIMAL;
    public static final String NAME_OWNER = "owner";


    private static final String TABLE_NAME = "sm_tb_product";

    /**
     * Create an array of type string containing all the fields of the sm_tb_product table.
     */
    private static final String[] FIELD_NAMES =
    {
        "sm_tb_product.prd_id"
        ,"sm_tb_product.prd_type_id"
        ,"sm_tb_product.name"
        ,"sm_tb_product.price"
        ,"sm_tb_product.price_vip"
        ,"sm_tb_product.suply_status"
        ,"sm_tb_product.format_desc"
        ,"sm_tb_product.detail_desc"
        ,"sm_tb_product.send_area"
        ,"sm_tb_product.send_desc"
        ,"sm_tb_product.prd_pic_file"
        ,"sm_tb_product.owner"
    };

    /**
     * Field that contains the comma separated fields of the sm_tb_product table.
     */
    private static final String ALL_FIELDS = "sm_tb_product.prd_id"
                            + ",sm_tb_product.prd_type_id"
                            + ",sm_tb_product.name"
                            + ",sm_tb_product.price"
                            + ",sm_tb_product.price_vip"
                            + ",sm_tb_product.suply_status"
                            + ",sm_tb_product.format_desc"
                            + ",sm_tb_product.detail_desc"
                            + ",sm_tb_product.send_area"
                            + ",sm_tb_product.send_desc"
                            + ",sm_tb_product.prd_pic_file"
                            + ",sm_tb_product.owner";

    private static SmTbProductManager singleton = new SmTbProductManager();

    /**
     * Get the SmTbProductManager singleton.
     *
     * @return SmTbProductManager
     */
    synchronized public static SmTbProductManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own SmTbProductManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(SmTbProductManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new SmTbProductBean instance.
     *
     * @return the new SmTbProductBean
     */
    public SmTbProductBean createSmTbProductBean()
    {
        return new SmTbProductBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a SmTbProductBean from the sm_tb_product using its key fields.
     *
     * @return a unique SmTbProductBean
     */
    //12
    public SmTbProductBean loadByPrimaryKey(Long prdId) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sm_tb_product WHERE sm_tb_product.prd_id=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, prdId);
            SmTbProductBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Long prdId) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from sm_tb_product WHERE sm_tb_product.prd_id=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, prdId);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from sm_tb_product.
     *
     * @return an array of SmTbProductManager pObject
     */
    //38
    public SmTbProductBean[] loadAll() throws SQLException
    {
        return loadAll(1, -1);
    }


    /**
     * Loads the given number of rows from sm_tb_product, given the start row.
     *
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @return an array of SmTbProductManager pObject
     */
    //38.5
    public SmTbProductBean[] loadAll(int startRow, int numRows) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sm_tb_product",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps, null, startRow, numRows);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of SmTbProductBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting SmTbProductBean table
     */
    //49
    public SmTbProductBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of SmTbProductBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting SmTbProductBean table
     */
    //51
    public SmTbProductBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        return loadByWhere(where, null, 1, -1);
    }

    /**
     * Retrieves an array of SmTbProductBean given a sql where clause and a list of fields, and startRow and numRows.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @param fieldList table of the field's associated constants
     * @return the resulting SmTbProductBean table
     */
    //51.5
    public SmTbProductBean[] loadByWhere(String where, int[] fieldList, int startRow, int numRows) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from sm_tb_product " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from sm_tb_product ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try
        {
            c = getConnection();
            pStatement = c.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            int count = 0;
            if (rs.absolute(startRow) && numRows!=0) {
                do
                {
                    if(fieldList == null)
                        v.add(decodeRow(rs));
                    else
                        v.add(decodeRow(rs, fieldList));
                    count++;
                } while ( (count<numRows||numRows<0) && rs.next() );
            }

            return (SmTbProductBean[])v.toArray(new SmTbProductBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from sm_tb_product table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the sm_tb_product table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records.
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM sm_tb_product " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the SmTbProductBean pObject into the database.
     *
     * @param pObject the SmTbProductBean pObject to be saved
     */
    //100
    public SmTbProductBean save(SmTbProductBean pObject) throws SQLException
    {
        if (!pObject.isModified()) {
            return pObject; // should not we log something ?
        }

        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into sm_tb_product (");

                if (pObject.isPrdIdModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("prd_id");
                    _dirtyCount++;
                }

                if (pObject.isPrdTypeIdModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("prd_type_id");
                    _dirtyCount++;
                }

                if (pObject.isNameModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("name");
                    _dirtyCount++;
                }

                if (pObject.isPriceModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("price");
                    _dirtyCount++;
                }

                if (pObject.isPriceVipModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("price_vip");
                    _dirtyCount++;
                }

                if (pObject.isSuplyStatusModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("suply_status");
                    _dirtyCount++;
                }

                if (pObject.isFormatDescModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("format_desc");
                    _dirtyCount++;
                }

                if (pObject.isDetailDescModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("detail_desc");
                    _dirtyCount++;
                }

                if (pObject.isSendAreaModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("send_area");
                    _dirtyCount++;
                }

                if (pObject.isSendDescModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("send_desc");
                    _dirtyCount++;
                }

                if (pObject.isPrdPicFileModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("prd_pic_file");
                    _dirtyCount++;
                }

                if (pObject.isOwnerModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("owner");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isPrdIdModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
                }

                if (pObject.isPrdTypeIdModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrdTypeId());
                }

                if (pObject.isNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getName());
                }

                if (pObject.isPriceModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrice());
                }

                if (pObject.isPriceVipModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPriceVip());
                }

                if (pObject.isSuplyStatusModified()) {
                    ps.setString(++_dirtyCount, pObject.getSuplyStatus());
                }

                if (pObject.isFormatDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getFormatDesc());
                }

                if (pObject.isDetailDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getDetailDesc());
                }

                if (pObject.isSendAreaModified()) {
                    ps.setString(++_dirtyCount, pObject.getSendArea());
                }

                if (pObject.isSendDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getSendDesc());
                }

                if (pObject.isPrdPicFileModified()) {
                    ps.setString(++_dirtyCount, pObject.getPrdPicFile());
                }

                if (pObject.isOwnerModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getOwner());
                }

                ps.executeUpdate();

                if (!pObject.isPrdIdModified())
                {
                    PreparedStatement ps2 = null;
                    ResultSet rs = null;
                    try {
                        ps2 = c.prepareStatement("SELECT last_insert_id()");
                        rs = ps2.executeQuery();
                        if(rs.next()) {
                            pObject.setPrdId(Manager.getLong(rs, 1));
                        } else {
                            getManager().log("ATTENTION: Could not retrieve generated key!");
                        }
                    } finally {
                        getManager().close(ps2, rs);
                    }
                }

                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else
            { // UPDATE
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE sm_tb_product SET ");
                boolean useComma=false;

                if (pObject.isPrdIdModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("prd_id").append("=?");
                }

                if (pObject.isPrdTypeIdModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("prd_type_id").append("=?");
                }

                if (pObject.isNameModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("name").append("=?");
                }

                if (pObject.isPriceModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("price").append("=?");
                }

                if (pObject.isPriceVipModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("price_vip").append("=?");
                }

                if (pObject.isSuplyStatusModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("suply_status").append("=?");
                }

                if (pObject.isFormatDescModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("format_desc").append("=?");
                }

                if (pObject.isDetailDescModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("detail_desc").append("=?");
                }

                if (pObject.isSendAreaModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("send_area").append("=?");
                }

                if (pObject.isSendDescModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("send_desc").append("=?");
                }

                if (pObject.isPrdPicFileModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("prd_pic_file").append("=?");
                }

                if (pObject.isOwnerModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("owner").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("sm_tb_product.prd_id=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isPrdIdModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
                }

                if (pObject.isPrdTypeIdModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPrdTypeId());
                }

                if (pObject.isNameModified()) {
                      ps.setString(++_dirtyCount, pObject.getName());
                }

                if (pObject.isPriceModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPrice());
                }

                if (pObject.isPriceVipModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPriceVip());
                }

                if (pObject.isSuplyStatusModified()) {
                      ps.setString(++_dirtyCount, pObject.getSuplyStatus());
                }

                if (pObject.isFormatDescModified()) {
                      ps.setString(++_dirtyCount, pObject.getFormatDesc());
                }

                if (pObject.isDetailDescModified()) {
                      ps.setString(++_dirtyCount, pObject.getDetailDesc());
                }

                if (pObject.isSendAreaModified()) {
                      ps.setString(++_dirtyCount, pObject.getSendArea());
                }

                if (pObject.isSendDescModified()) {
                      ps.setString(++_dirtyCount, pObject.getSendDesc());
                }

                if (pObject.isPrdPicFileModified()) {
                      ps.setString(++_dirtyCount, pObject.getPrdPicFile());
                }

                if (pObject.isOwnerModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getOwner());
                }

                if (_dirtyCount == 0) {
                     return pObject;
                }

                Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }

            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of SmTbProductBean pObjects into the database.
     *
     * @param pObjects the SmTbProductBean pObject table to be saved
     * @return the saved SmTbProductBean array.
     */
    //65
    public SmTbProductBean[] save(SmTbProductBean[] pObjects) throws SQLException
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique SmTbProductBean pObject from a template one giving a c
     *
     * @param pObject the SmTbProductBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public SmTbProductBean loadUniqueUsingTemplate(SmTbProductBean pObject) throws SQLException
    {
         SmTbProductBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of SmTbProductBean from a template one.
     *
     * @param pObject the SmTbProductBean template to look for
     * @return all the SmTbProductBean matching the template
     */
    //88
    public SmTbProductBean[] loadUsingTemplate(SmTbProductBean pObject) throws SQLException
    {
        return loadUsingTemplate(pObject, 1, -1);
    }
    /**
     * Loads an array of SmTbProductBean from a template one, given the start row and number of rows.
     *
     * @param pObject the SmTbProductBean template to look for
     * @param startRow the start row to be used (first row = 1, last row=-1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @return all the SmTbProductBean matching the template
     */
    //88.5
    public SmTbProductBean[] loadUsingTemplate(SmTbProductBean pObject, int startRow, int numRows) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from sm_tb_product WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;

             if (pObject.isPrdIdModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_id= ?");
             }

             if (pObject.isPrdTypeIdModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_type_id= ?");
             }

             if (pObject.isNameModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("name= ?");
             }

             if (pObject.isPriceModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("price= ?");
             }

             if (pObject.isPriceVipModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("price_vip= ?");
             }

             if (pObject.isSuplyStatusModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("suply_status= ?");
             }

             if (pObject.isFormatDescModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("format_desc= ?");
             }

             if (pObject.isDetailDescModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("detail_desc= ?");
             }

             if (pObject.isSendAreaModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("send_area= ?");
             }

             if (pObject.isSendDescModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("send_desc= ?");
             }

             if (pObject.isPrdPicFileModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_pic_file= ?");
             }

             if (pObject.isOwnerModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("owner= ?");
             }

             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;

             if (pObject.isPrdIdModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
             }

             if (pObject.isPrdTypeIdModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPrdTypeId());
             }

             if (pObject.isNameModified()) {
                 ps.setString(++_dirtyCount, pObject.getName());
             }

             if (pObject.isPriceModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPrice());
             }

             if (pObject.isPriceVipModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPriceVip());
             }

             if (pObject.isSuplyStatusModified()) {
                 ps.setString(++_dirtyCount, pObject.getSuplyStatus());
             }

             if (pObject.isFormatDescModified()) {
                 ps.setString(++_dirtyCount, pObject.getFormatDesc());
             }

             if (pObject.isDetailDescModified()) {
                 ps.setString(++_dirtyCount, pObject.getDetailDesc());
             }

             if (pObject.isSendAreaModified()) {
                 ps.setString(++_dirtyCount, pObject.getSendArea());
             }

             if (pObject.isSendDescModified()) {
                 ps.setString(++_dirtyCount, pObject.getSendDesc());
             }

             if (pObject.isPrdPicFileModified()) {
                 ps.setString(++_dirtyCount, pObject.getPrdPicFile());
             }

             if (pObject.isOwnerModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getOwner());
             }

             ps.executeQuery();
             return loadByPreparedStatement(ps, null, startRow, numRows);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a SmTbProductBean template.
     *
     * @param pObject the SmTbProductBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(SmTbProductBean pObject) throws SQLException
    {
        if (pObject.isPrdIdInitialized())
            return deleteByPrimaryKey(pObject.getPrdId());

        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;

        try
        {
            sql = new StringBuffer("DELETE FROM sm_tb_product WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isPrdIdInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("prd_id").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isPrdTypeIdInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("prd_type_id").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isNameInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("name").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isPriceInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("price").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isPriceVipInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("price_vip").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isSuplyStatusInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("suply_status").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isFormatDescInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("format_desc").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isDetailDescInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("detail_desc").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isSendAreaInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("send_area").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isSendDescInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("send_desc").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isPrdPicFileInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("prd_pic_file").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isOwnerInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("owner").append("=?");
                _dirtyAnd ++;
            }

            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;

            if (pObject.isPrdIdInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
            }

            if (pObject.isPrdTypeIdInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPrdTypeId());
            }

            if (pObject.isNameInitialized()) {
                ps.setString(++_dirtyCount, pObject.getName());
            }

            if (pObject.isPriceInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPrice());
            }

            if (pObject.isPriceVipInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPriceVip());
            }

            if (pObject.isSuplyStatusInitialized()) {
                ps.setString(++_dirtyCount, pObject.getSuplyStatus());
            }

            if (pObject.isFormatDescInitialized()) {
                ps.setString(++_dirtyCount, pObject.getFormatDesc());
            }

            if (pObject.isDetailDescInitialized()) {
                ps.setString(++_dirtyCount, pObject.getDetailDesc());
            }

            if (pObject.isSendAreaInitialized()) {
                ps.setString(++_dirtyCount, pObject.getSendArea());
            }

            if (pObject.isSendDescInitialized()) {
                ps.setString(++_dirtyCount, pObject.getSendDesc());
            }

            if (pObject.isPrdPicFileInitialized()) {
                ps.setString(++_dirtyCount, pObject.getPrdPicFile());
            }

            if (pObject.isOwnerInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getOwner());
            }

            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table sm_tb_product.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table sm_tb_product with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from sm_tb_product " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try
        {
            int iReturn = -1;
            c = getConnection();
            pStatement = c.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table sm_tb_product with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific SmTbProductBean pObject given a c
     *
     * @param pObject the SmTbProductBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(SmTbProductBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;

        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from sm_tb_product WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;

                if (pObject.isPrdIdModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_id= ?");
                }

                if (pObject.isPrdTypeIdModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_type_id= ?");
                }

                if (pObject.isNameModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("name= ?");
                }

                if (pObject.isPriceModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("price= ?");
                }

                if (pObject.isPriceVipModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("price_vip= ?");
                }

                if (pObject.isSuplyStatusModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("suply_status= ?");
                }

                if (pObject.isFormatDescModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("format_desc= ?");
                }

                if (pObject.isDetailDescModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("detail_desc= ?");
                }

                if (pObject.isSendAreaModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("send_area= ?");
                }

                if (pObject.isSendDescModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("send_desc= ?");
                }

                if (pObject.isPrdPicFileModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("prd_pic_file= ?");
                }

                if (pObject.isOwnerModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("owner= ?");
                }

                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");

                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);

                _dirtyCount = 0;

                if (pObject.isPrdIdModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrdId());
                }

                if (pObject.isPrdTypeIdModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrdTypeId());
                }

                if (pObject.isNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getName());
                }

                if (pObject.isPriceModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPrice());
                }

                if (pObject.isPriceVipModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPriceVip());
                }

                if (pObject.isSuplyStatusModified()) {
                    ps.setString(++_dirtyCount, pObject.getSuplyStatus());
                }

                if (pObject.isFormatDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getFormatDesc());
                }

                if (pObject.isDetailDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getDetailDesc());
                }

                if (pObject.isSendAreaModified()) {
                    ps.setString(++_dirtyCount, pObject.getSendArea());
                }

                if (pObject.isSendDescModified()) {
                    ps.setString(++_dirtyCount, pObject.getSendDesc());
                }

                if (pObject.isPrdPicFileModified()) {
                    ps.setString(++_dirtyCount, pObject.getPrdPicFile());
                }

                if (pObject.isOwnerModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getOwner());
                }

                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the sm_tb_product on a SmTbProductBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting SmTbProductBean pObject
     */
    //72
    public SmTbProductBean decodeRow(ResultSet rs) throws SQLException
    {
        SmTbProductBean pObject = createSmTbProductBean();
        pObject.setPrdId(Manager.getLong(rs, 1));
        pObject.setPrdTypeId(Manager.getLong(rs, 2));
        pObject.setName(rs.getString(3));
        pObject.setPrice(Manager.getLong(rs, 4));
        pObject.setPriceVip(Manager.getLong(rs, 5));
        pObject.setSuplyStatus(rs.getString(6));
        pObject.setFormatDesc(rs.getString(7));
        pObject.setDetailDesc(rs.getString(8));
        pObject.setSendArea(rs.getString(9));
        pObject.setSendDesc(rs.getString(10));
        pObject.setPrdPicFile(rs.getString(11));
        pObject.setOwner(Manager.getLong(rs, 12));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the sm_tb_product table on a SmTbProductBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting SmTbProductBean pObject
     */
    //73
    public SmTbProductBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        SmTbProductBean pObject = createSmTbProductBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_PRD_ID:
                    ++pos;
                    pObject.setPrdId(Manager.getLong(rs, pos));
                    break;
                case ID_PRD_TYPE_ID:
                    ++pos;
                    pObject.setPrdTypeId(Manager.getLong(rs, pos));
                    break;
                case ID_NAME:
                    ++pos;
                    pObject.setName(rs.getString(pos));
                    break;
                case ID_PRICE:
                    ++pos;
                    pObject.setPrice(Manager.getLong(rs, pos));
                    break;
                case ID_PRICE_VIP:
                    ++pos;
                    pObject.setPriceVip(Manager.getLong(rs, pos));
                    break;
                case ID_SUPLY_STATUS:
                    ++pos;
                    pObject.setSuplyStatus(rs.getString(pos));
                    break;
                case ID_FORMAT_DESC:
                    ++pos;
                    pObject.setFormatDesc(rs.getString(pos));
                    break;
                case ID_DETAIL_DESC:
                    ++pos;
                    pObject.setDetailDesc(rs.getString(pos));
                    break;
                case ID_SEND_AREA:
                    ++pos;
                    pObject.setSendArea(rs.getString(pos));
                    break;
                case ID_SEND_DESC:
                    ++pos;
                    pObject.setSendDesc(rs.getString(pos));
                    break;
                case ID_PRD_PIC_FILE:
                    ++pos;
                    pObject.setPrdPicFile(rs.getString(pos));
                    break;
                case ID_OWNER:
                    ++pos;
                    pObject.setOwner(Manager.getLong(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of SmTbProductBean
     */
    //41
    public SmTbProductBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of SmTbProductBean
     */
    public SmTbProductBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        return loadByPreparedStatement(ps, fieldList, 1, -1);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved,
     * and specifying the start row and the number of rows.
     *
     * @param ps the PreparedStatement to be used
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @param fieldList table of the field's associated constants
     * @return an array of SmTbProductBean
     */
    //41.5
    public SmTbProductBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList, int startRow, int numRows) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            int count = 0;
            if (rs.absolute(startRow) && numRows!=0) {
                do
                {
                    if(fieldList == null)
                        v.add(decodeRow(rs));
                    else
                        v.add(decodeRow(rs, fieldList));
                    count++;
                } while ( (count<numRows||numRows<0) && rs.next() );
            }
            return (SmTbProductBean[])v.toArray(new SmTbProductBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER
    ///////////////////////////////////////////////////////////////////////
    private SmTbProductListener listener = null;

    /**
     * Registers a unique SmTbProductListener listener.
     */
    //66.5
    public void registerListener(SmTbProductListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the SmTbProductBean pObject.
     *
     * @param pObject the SmTbProductBean pObject to be saved
     */
    //67
    void beforeInsert(SmTbProductBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the SmTbProductBean pObject.
     *
     * @param pObject the SmTbProductBean pObject to be saved
     */
    //68
    void afterInsert(SmTbProductBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the SmTbProductBean pObject.
     *
     * @param pObject the SmTbProductBean pObject to be updated
     */
    //69
    void beforeUpdate(SmTbProductBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the SmTbProductBean pObject.
     *
     * @param pObject the SmTbProductBean pObject to be updated
     */
    //70
    void afterUpdate(SmTbProductBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 
}
