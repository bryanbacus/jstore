// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: com.mysql.jdbc.Driver
//
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.shenming.sms.dc.sql2java;

import java.sql.*;
// imports+ 

// imports- 

/**
 * Handles database calls for the sm_vw_account_sattlement table.
 */
public class SmVwAccountSattlementManager
// extends+ 

// extends- 
{

    /**
     * Column sm_vip_seq of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_SM_VIP_SEQ = 0;
    public static final int TYPE_SM_VIP_SEQ = Types.DECIMAL;
    public static final String NAME_SM_VIP_SEQ = "sm_vip_seq";

    /**
     * Column username of type Types.VARCHAR mapped to String.
     */
    public static final int ID_USERNAME = 1;
    public static final int TYPE_USERNAME = Types.VARCHAR;
    public static final String NAME_USERNAME = "username";

    /**
     * Column vip_cht_name of type Types.VARCHAR mapped to String.
     */
    public static final int ID_VIP_CHT_NAME = 2;
    public static final int TYPE_VIP_CHT_NAME = Types.VARCHAR;
    public static final String NAME_VIP_CHT_NAME = "vip_cht_name";

    /**
     * Column pay_sum of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_PAY_SUM = 3;
    public static final int TYPE_PAY_SUM = Types.DECIMAL;
    public static final String NAME_PAY_SUM = "pay_sum";

    /**
     * Column buy_sum of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_BUY_SUM = 4;
    public static final int TYPE_BUY_SUM = Types.DECIMAL;
    public static final String NAME_BUY_SUM = "buy_sum";

    /**
     * Column settlement of type Types.DECIMAL mapped to Long.
     */
    public static final int ID_SETTLEMENT = 5;
    public static final int TYPE_SETTLEMENT = Types.DECIMAL;
    public static final String NAME_SETTLEMENT = "settlement";


    private static final String TABLE_NAME = "sm_vw_account_sattlement";

    /**
     * Create an array of type string containing all the fields of the sm_vw_account_sattlement table.
     */
    private static final String[] FIELD_NAMES =
    {
        "sm_vw_account_sattlement.sm_vip_seq"
        ,"sm_vw_account_sattlement.username"
        ,"sm_vw_account_sattlement.vip_cht_name"
        ,"sm_vw_account_sattlement.pay_sum"
        ,"sm_vw_account_sattlement.buy_sum"
        ,"sm_vw_account_sattlement.settlement"
    };

    /**
     * Field that contains the comma separated fields of the sm_vw_account_sattlement table.
     */
    private static final String ALL_FIELDS = "sm_vw_account_sattlement.sm_vip_seq"
                            + ",sm_vw_account_sattlement.username"
                            + ",sm_vw_account_sattlement.vip_cht_name"
                            + ",sm_vw_account_sattlement.pay_sum"
                            + ",sm_vw_account_sattlement.buy_sum"
                            + ",sm_vw_account_sattlement.settlement";

    private static SmVwAccountSattlementManager singleton = new SmVwAccountSattlementManager();

    /**
     * Get the SmVwAccountSattlementManager singleton.
     *
     * @return SmVwAccountSattlementManager
     */
    synchronized public static SmVwAccountSattlementManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own SmVwAccountSattlementManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(SmVwAccountSattlementManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new SmVwAccountSattlementBean instance.
     *
     * @return the new SmVwAccountSattlementBean
     */
    public SmVwAccountSattlementBean createSmVwAccountSattlementBean()
    {
        return new SmVwAccountSattlementBean();
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from sm_vw_account_sattlement.
     *
     * @return an array of SmVwAccountSattlementManager pObject
     */
    //38
    public SmVwAccountSattlementBean[] loadAll() throws SQLException
    {
        return loadAll(1, -1);
    }


    /**
     * Loads the given number of rows from sm_vw_account_sattlement, given the start row.
     *
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @return an array of SmVwAccountSattlementManager pObject
     */
    //38.5
    public SmVwAccountSattlementBean[] loadAll(int startRow, int numRows) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM sm_vw_account_sattlement",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps, null, startRow, numRows);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of SmVwAccountSattlementBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting SmVwAccountSattlementBean table
     */
    //49
    public SmVwAccountSattlementBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of SmVwAccountSattlementBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting SmVwAccountSattlementBean table
     */
    //51
    public SmVwAccountSattlementBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        return loadByWhere(where, null, 1, -1);
    }

    /**
     * Retrieves an array of SmVwAccountSattlementBean given a sql where clause and a list of fields, and startRow and numRows.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @param fieldList table of the field's associated constants
     * @return the resulting SmVwAccountSattlementBean table
     */
    //51.5
    public SmVwAccountSattlementBean[] loadByWhere(String where, int[] fieldList, int startRow, int numRows) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from sm_vw_account_sattlement " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from sm_vw_account_sattlement ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try
        {
            c = getConnection();
            pStatement = c.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            int count = 0;
            if (rs.absolute(startRow) && numRows!=0) {
                do
                {
                    if(fieldList == null)
                        v.add(decodeRow(rs));
                    else
                        v.add(decodeRow(rs, fieldList));
                    count++;
                } while ( (count<numRows||numRows<0) && rs.next() );
            }

            return (SmVwAccountSattlementBean[])v.toArray(new SmVwAccountSattlementBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from sm_vw_account_sattlement table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the sm_vw_account_sattlement table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records.
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM sm_vw_account_sattlement " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the SmVwAccountSattlementBean pObject into the database.
     *
     * @param pObject the SmVwAccountSattlementBean pObject to be saved
     */
    //100
    public SmVwAccountSattlementBean save(SmVwAccountSattlementBean pObject) throws SQLException
    {
        if (!pObject.isModified()) {
            return pObject; // should not we log something ?
        }

        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into sm_vw_account_sattlement (");

                if (pObject.isSmVipSeqModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("sm_vip_seq");
                    _dirtyCount++;
                }

                if (pObject.isUsernameModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("username");
                    _dirtyCount++;
                }

                if (pObject.isVipChtNameModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("vip_cht_name");
                    _dirtyCount++;
                }

                if (pObject.isPaySumModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("pay_sum");
                    _dirtyCount++;
                }

                if (pObject.isBuySumModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("buy_sum");
                    _dirtyCount++;
                }

                if (pObject.isSettlementModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("settlement");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isSmVipSeqModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSmVipSeq());
                }

                if (pObject.isUsernameModified()) {
                    ps.setString(++_dirtyCount, pObject.getUsername());
                }

                if (pObject.isVipChtNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getVipChtName());
                }

                if (pObject.isPaySumModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPaySum());
                }

                if (pObject.isBuySumModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getBuySum());
                }

                if (pObject.isSettlementModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSettlement());
                }

                ps.executeUpdate();

                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else
            { // UPDATE

                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE sm_vw_account_sattlement SET ");
                boolean useComma=false;

                if (pObject.isSmVipSeqModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("sm_vip_seq").append("=?");
                }

                if (pObject.isUsernameModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("username").append("=?");
                }

                if (pObject.isVipChtNameModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("vip_cht_name").append("=?");
                }

                if (pObject.isPaySumModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("pay_sum").append("=?");
                }

                if (pObject.isBuySumModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("buy_sum").append("=?");
                }

                if (pObject.isSettlementModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("settlement").append("=?");
                }
                _sql.append("");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isSmVipSeqModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getSmVipSeq());
                }

                if (pObject.isUsernameModified()) {
                      ps.setString(++_dirtyCount, pObject.getUsername());
                }

                if (pObject.isVipChtNameModified()) {
                      ps.setString(++_dirtyCount, pObject.getVipChtName());
                }

                if (pObject.isPaySumModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getPaySum());
                }

                if (pObject.isBuySumModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getBuySum());
                }

                if (pObject.isSettlementModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getSettlement());
                }

                if (_dirtyCount == 0) {
                     return pObject;
                }

                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }

            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of SmVwAccountSattlementBean pObjects into the database.
     *
     * @param pObjects the SmVwAccountSattlementBean pObject table to be saved
     * @return the saved SmVwAccountSattlementBean array.
     */
    //65
    public SmVwAccountSattlementBean[] save(SmVwAccountSattlementBean[] pObjects) throws SQLException
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique SmVwAccountSattlementBean pObject from a template one giving a c
     *
     * @param pObject the SmVwAccountSattlementBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public SmVwAccountSattlementBean loadUniqueUsingTemplate(SmVwAccountSattlementBean pObject) throws SQLException
    {
         SmVwAccountSattlementBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of SmVwAccountSattlementBean from a template one.
     *
     * @param pObject the SmVwAccountSattlementBean template to look for
     * @return all the SmVwAccountSattlementBean matching the template
     */
    //88
    public SmVwAccountSattlementBean[] loadUsingTemplate(SmVwAccountSattlementBean pObject) throws SQLException
    {
        return loadUsingTemplate(pObject, 1, -1);
    }
    /**
     * Loads an array of SmVwAccountSattlementBean from a template one, given the start row and number of rows.
     *
     * @param pObject the SmVwAccountSattlementBean template to look for
     * @param startRow the start row to be used (first row = 1, last row=-1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @return all the SmVwAccountSattlementBean matching the template
     */
    //88.5
    public SmVwAccountSattlementBean[] loadUsingTemplate(SmVwAccountSattlementBean pObject, int startRow, int numRows) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from sm_vw_account_sattlement WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;

             if (pObject.isSmVipSeqModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sm_vip_seq= ?");
             }

             if (pObject.isUsernameModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("username= ?");
             }

             if (pObject.isVipChtNameModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("vip_cht_name= ?");
             }

             if (pObject.isPaySumModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("pay_sum= ?");
             }

             if (pObject.isBuySumModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("buy_sum= ?");
             }

             if (pObject.isSettlementModified()) {
                 _dirtyCount ++;
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("settlement= ?");
             }

             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;

             if (pObject.isSmVipSeqModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getSmVipSeq());
             }

             if (pObject.isUsernameModified()) {
                 ps.setString(++_dirtyCount, pObject.getUsername());
             }

             if (pObject.isVipChtNameModified()) {
                 ps.setString(++_dirtyCount, pObject.getVipChtName());
             }

             if (pObject.isPaySumModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getPaySum());
             }

             if (pObject.isBuySumModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getBuySum());
             }

             if (pObject.isSettlementModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getSettlement());
             }

             ps.executeQuery();
             return loadByPreparedStatement(ps, null, startRow, numRows);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a SmVwAccountSattlementBean template.
     *
     * @param pObject the SmVwAccountSattlementBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(SmVwAccountSattlementBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;

        try
        {
            sql = new StringBuffer("DELETE FROM sm_vw_account_sattlement WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isSmVipSeqInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("sm_vip_seq").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isUsernameInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("username").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isVipChtNameInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("vip_cht_name").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isPaySumInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("pay_sum").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isBuySumInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("buy_sum").append("=?");
                _dirtyAnd ++;
            }

            if (pObject.isSettlementInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("settlement").append("=?");
                _dirtyAnd ++;
            }

            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;

            if (pObject.isSmVipSeqInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getSmVipSeq());
            }

            if (pObject.isUsernameInitialized()) {
                ps.setString(++_dirtyCount, pObject.getUsername());
            }

            if (pObject.isVipChtNameInitialized()) {
                ps.setString(++_dirtyCount, pObject.getVipChtName());
            }

            if (pObject.isPaySumInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getPaySum());
            }

            if (pObject.isBuySumInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getBuySum());
            }

            if (pObject.isSettlementInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getSettlement());
            }

            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table sm_vw_account_sattlement.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table sm_vw_account_sattlement with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from sm_vw_account_sattlement " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try
        {
            int iReturn = -1;
            c = getConnection();
            pStatement = c.createStatement(ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table sm_vw_account_sattlement with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific SmVwAccountSattlementBean pObject given a c
     *
     * @param pObject the SmVwAccountSattlementBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(SmVwAccountSattlementBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;

        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from sm_vw_account_sattlement WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;

                if (pObject.isSmVipSeqModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sm_vip_seq= ?");
                }

                if (pObject.isUsernameModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("username= ?");
                }

                if (pObject.isVipChtNameModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("vip_cht_name= ?");
                }

                if (pObject.isPaySumModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("pay_sum= ?");
                }

                if (pObject.isBuySumModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("buy_sum= ?");
                }

                if (pObject.isSettlementModified()) {
                    _dirtyCount++;
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("settlement= ?");
                }

                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");

                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);

                _dirtyCount = 0;

                if (pObject.isSmVipSeqModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSmVipSeq());
                }

                if (pObject.isUsernameModified()) {
                    ps.setString(++_dirtyCount, pObject.getUsername());
                }

                if (pObject.isVipChtNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getVipChtName());
                }

                if (pObject.isPaySumModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getPaySum());
                }

                if (pObject.isBuySumModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getBuySum());
                }

                if (pObject.isSettlementModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSettlement());
                }

                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the sm_vw_account_sattlement on a SmVwAccountSattlementBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting SmVwAccountSattlementBean pObject
     */
    //72
    public SmVwAccountSattlementBean decodeRow(ResultSet rs) throws SQLException
    {
        SmVwAccountSattlementBean pObject = createSmVwAccountSattlementBean();
        pObject.setSmVipSeq(Manager.getLong(rs, 1));
        pObject.setUsername(rs.getString(2));
        pObject.setVipChtName(rs.getString(3));
        pObject.setPaySum(Manager.getLong(rs, 4));
        pObject.setBuySum(Manager.getLong(rs, 5));
        pObject.setSettlement(Manager.getLong(rs, 6));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the sm_vw_account_sattlement table on a SmVwAccountSattlementBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting SmVwAccountSattlementBean pObject
     */
    //73
    public SmVwAccountSattlementBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        SmVwAccountSattlementBean pObject = createSmVwAccountSattlementBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_SM_VIP_SEQ:
                    ++pos;
                    pObject.setSmVipSeq(Manager.getLong(rs, pos));
                    break;
                case ID_USERNAME:
                    ++pos;
                    pObject.setUsername(rs.getString(pos));
                    break;
                case ID_VIP_CHT_NAME:
                    ++pos;
                    pObject.setVipChtName(rs.getString(pos));
                    break;
                case ID_PAY_SUM:
                    ++pos;
                    pObject.setPaySum(Manager.getLong(rs, pos));
                    break;
                case ID_BUY_SUM:
                    ++pos;
                    pObject.setBuySum(Manager.getLong(rs, pos));
                    break;
                case ID_SETTLEMENT:
                    ++pos;
                    pObject.setSettlement(Manager.getLong(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of SmVwAccountSattlementBean
     */
    //41
    public SmVwAccountSattlementBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of SmVwAccountSattlementBean
     */
    public SmVwAccountSattlementBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        return loadByPreparedStatement(ps, fieldList, 1, -1);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved,
     * and specifying the start row and the number of rows.
     *
     * @param ps the PreparedStatement to be used
     * @param startRow the start row to be used (first row = 1, last row = -1)
     * @param numRows the number of rows to be retrieved (all rows = a negative number)
     * @param fieldList table of the field's associated constants
     * @return an array of SmVwAccountSattlementBean
     */
    //41.5
    public SmVwAccountSattlementBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList, int startRow, int numRows) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            int count = 0;
            if (rs.absolute(startRow) && numRows!=0) {
                do
                {
                    if(fieldList == null)
                        v.add(decodeRow(rs));
                    else
                        v.add(decodeRow(rs, fieldList));
                    count++;
                } while ( (count<numRows||numRows<0) && rs.next() );
            }
            return (SmVwAccountSattlementBean[])v.toArray(new SmVwAccountSattlementBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER
    ///////////////////////////////////////////////////////////////////////
    private SmVwAccountSattlementListener listener = null;

    /**
     * Registers a unique SmVwAccountSattlementListener listener.
     */
    //66.5
    public void registerListener(SmVwAccountSattlementListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the SmVwAccountSattlementBean pObject.
     *
     * @param pObject the SmVwAccountSattlementBean pObject to be saved
     */
    //67
    void beforeInsert(SmVwAccountSattlementBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the SmVwAccountSattlementBean pObject.
     *
     * @param pObject the SmVwAccountSattlementBean pObject to be saved
     */
    //68
    void afterInsert(SmVwAccountSattlementBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the SmVwAccountSattlementBean pObject.
     *
     * @param pObject the SmVwAccountSattlementBean pObject to be updated
     */
    //69
    void beforeUpdate(SmVwAccountSattlementBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the SmVwAccountSattlementBean pObject.
     *
     * @param pObject the SmVwAccountSattlementBean pObject to be updated
     */
    //70
    void afterUpdate(SmVwAccountSattlementBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 
}
